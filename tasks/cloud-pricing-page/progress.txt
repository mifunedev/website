# progress

## US-001 — Apache-2.0 licence sweep — PASS (commit d669291)

15 MIT claims across 9 files replaced with "Apache-2.0 licensed"; the
delegate's own grep found exactly 15, matching the audited list with no
extras and no stale entries. public/llm.txt regenerated via prebuild,
never hand-edited.

Validated by the orchestrator against the diff itself: 15 "+Apache-2.0"
insertions, zero MIT remaining, 10 files changed, public/sw.js excluded.
lint 0, tsc 0, prettier clean on every touched file.

Two baseline defects surfaced and handled:
- npm run lint failed on untouched HEAD (nested-worktree ESLint
  resolution). Fixed in b0eeba9, committed separately.
- npx prettier --check src/ scripts/ fails on 38 files on untouched
  HEAD. Pre-existing drift; PRD criterion narrowed to touched files.

Left alone deliberately: schema.ts:101 links `license` to
/blob/main/LICENSE upstream — correct only if the relicensed LICENSE
landed at that path, unverifiable from this repo. Flagged, not changed.

## US-002/003/004 — pricing source, hero, price cards — PASS (commit 63e1ca1)

/pricing now publishes $0.0384 / $0.0692 / $0.5046 per whole running
hour, states AI usage is not included above the prices, and explains the
billing mechanic before showing numbers. No monthly price anywhere.

Orchestrator validation went to the RENDERED HTML, not the source:
- .next/server/app/pricing.html contains the three hourly prices, the
  AI-not-included sentence, and the whole-running-UTC-hours language.
- Zero matches for a monthly price or "/mo" in the rendered output.
- lint 0, tsc 0, prettier clean on both files, real build exit 0.

Delegate's browser pass was unusually good and is trusted on the points
only a browser can show: grid still 2 columns at 800px (md genuinely
skipped), no horizontal scroll at 375/700/800/1280, smallest link 44px,
accent-on-raised contrast measured rather than assumed.

Orchestrator fix: the breadcrumb JSON-LD still named this page "Deploy
Open Harness" — structured data contradicting the page, the same class
of machine-readable defect as the MIT claims US-001 removed. The
delegate left it on a literal reading of "preserve the three JsonLd
blocks"; preserving a block means not deleting it, not freezing its
contents. Corrected to "Pricing".

Open judgement call left for US-007: the delegate dropped the hero's
"Read the Open Harness docs" link (docs remains linked in nav and
footer). Recorded rather than reverted.
